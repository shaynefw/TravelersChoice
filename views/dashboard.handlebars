{{!-- This is for "/dashboard" --}}

<section class="new-post-btn-section">
  <div>
    <h2>List Of Your Reviews</h2>
    <div>
  {{#each reviews as |review|}}
  {{> dashboard-reviews}}
{{/each}}
    </div>
  </div>
  <button id="newpost" class="btn-new-post">
    <a href="/dashboard/newpost">Create a new post</a>
  </button>
</section>